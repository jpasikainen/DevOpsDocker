The only way to make this work was to first create text.log using touch.
Trying to mount the directory caused errors.
docker run -v "$(pwd)/text.log:/usr/src/app/text.log" devopsdockeruh/simple-web-service
