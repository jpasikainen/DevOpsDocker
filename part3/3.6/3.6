Run frontend with
docker run --rm -it -p 5000:80 frontend

The image size is now 23.8MB

Run backend with
docker run --rm -it -p 8080:8080 backend
and navigate to localhost:5000

The image size is now 18.7MB
